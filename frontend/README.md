# GoldVault Frontend

This folder will hold the Next.js App Router project for interacting with the deployed GoldVault contracts. 

The `contracts.json` file injected during Foundry deployment will be loaded to automatically map contract addresses dynamically per deployment.
